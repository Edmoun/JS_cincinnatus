
// la funcion sort: esta me ordena el array de manera alfabetica.
// la funcion length: me dice el tamaño del arreglo.
// la funcion reverse: me ordena el array de atra para adelante.
// la funcion push: esta me permite añadir otro elemento a mi array.
// la funcion shift: esta me permite poder eliminar algun elmento del array.
// la funcion pop: me elimina el ultimo elemento del arrays.
// la funcion indexOf: me dice la posicion en la que se encuentran los elementos de mi arrays.
// la funcion unshift: esta me permite agregar elementos al inicio de mi array.

let grupo_a = [1,2,3];
let grupo_b = [4,5,6];
console.log(grupo_a.concat(grupo_b));